# If you want to use this, you must configure the yaml form builder in the configuration.
#
# Sample mapping to POPO style entity model
# Acme.MyApp.Entity.Category.form.yml
RindowTest\Web\Form\Builder\YamlBuilderTest\Normal:
  attributes:
    action: "/app/form"
    method: post
  properties:
    email:
      type: email
      label: Email
